#include <stdio.h>

void help()
{
	/* puts удобная функция, когда нужно просто выводить какой-то текст, она ставит \n автоматически */
	puts("\tПрограмма демонстрации возможностей библиотеки readline");
	puts("\tДля начала работы введите одну из следующих команд:");
	puts("\thelp - вывести эту справку");
	puts("\tstrlen - подсчитать количество символов в строке");
	puts("\tquit/exit или Ctrl+D - выход из программы");
}
